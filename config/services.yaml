services:
    _defaults:
        autowire: true
        autoconfigure: true

    Smsh\TicketingBridge\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/SmshTicketingBridgeBundle.php'

    # Consumer MiniProgram QRs use WeChat Pay credentials unless a WeChat integration slug is set.
